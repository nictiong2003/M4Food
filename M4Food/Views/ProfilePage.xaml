<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="M4Food.Views.ProfilePage"
             Title="My Profile"
             BackgroundColor="#FAF9F6">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                <Image Source="user_icon.png" HeightRequest="100" WidthRequest="100" HorizontalOptions="Center" />
                <Label Text="John Doe" FontSize="24" FontAttributes="Bold" TextColor="Black" HorizontalOptions="Center"/>
                <Label Text="john.doe@example.com" FontSize="14" TextColor="Gray" HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <Button Text="✏️ Edit Profile" 
                    Clicked="OnEditProfileClicked"
                    BackgroundColor="White" TextColor="Black" BorderColor="Gray" BorderWidth="1" CornerRadius="10"/>

            <Button Text="📦 My Orders" 
                    BackgroundColor="White" TextColor="Black" BorderColor="Gray" BorderWidth="1" CornerRadius="10"/>

            <Button Text="Log Out" 
                    Clicked="OnLogoutClicked"
                    BackgroundColor="#F4D03F" TextColor="Black" FontAttributes="Bold" CornerRadius="25" Margin="0,30,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>